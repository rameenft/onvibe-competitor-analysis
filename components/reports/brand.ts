// OnVibe's brand colors (pulled from onvibe.co's own stylesheet), used to
// give the customer-facing report OnVibe letterhead styling -- OnVibe is
// the platform producing these reports for its SMB clients, so the brand
// belongs on the report chrome (header, section accents, plan phases),
// not on the analyzed client's own name/identity.
export const ONVIBE_BRAND = {
  yellow: "#FFF200",
  teal: "#01959F",
  coral: "#FF554B",
  ink: "#0b0b0b",
} as const;
