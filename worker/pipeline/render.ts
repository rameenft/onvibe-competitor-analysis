// Placeholder — replaced by the Playwright print-to-PDF pipeline once the
// report pages exist. Kept as a no-op for now so the rest of the pipeline
// can run end-to-end before that piece is built.
export async function renderReports(_analysisId: string): Promise<void> {
  return;
}
